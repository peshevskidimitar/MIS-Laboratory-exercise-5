class TimetableItem {
  final String id;
  final String subject;
  final DateTime time;
  final String userId;
  final String location;

  TimetableItem(this.id, this.subject, this.time, this.userId, this.location);
}
